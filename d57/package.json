{
  "name": "webgpu-path-tracer",
  "version": "1.0.0",
  "description": "Multi-GPU Path Tracer with WebGPU",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.40",
    "typescript": "^5.3.3",
    "vite": "^5.0.12"
  }
}
